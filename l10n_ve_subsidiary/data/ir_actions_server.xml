
<odoo>
	<data>

        <record model="ir.actions.server" id="correction_subsidiary_account_payment">
            <field name="name">correction subsidiary account payment</field>
            <field name="model_id" ref="model_account_payment"/>
            <field name="binding_model_id" ref="model_account_payment" />
            <field name="state">code</field>
            <field name="code">
                if records:
                    for record in records:
                        record.correccion_subsidiary_account_payment()
            </field>
        </record>

        <record model="ir.actions.server" id="correction_subsidiary_account_move">
            <field name="name">correction subsidiary account move</field>
            <field name="model_id" ref="model_account_move"/>
            <field name="binding_model_id" ref="model_account_move" />
            <field name="state">code</field>
            <field name="code">
                if records:
                    for record in records:
                        record.correccion_subsidiary()
            </field>
        </record>

        <record model="ir.actions.server" id="correction_subsidiary_sale_order">
            <field name="name">correction subsidiary sale order</field>
            <field name="model_id" ref="model_sale_order"/>
            <field name="binding_model_id" ref="model_sale_order" />
            <field name="state">code</field>
            <field name="code">
                if records:
                    for record in records:
                        record.correccion_subsidiary_order()
            </field>
        </record>
        
	</data>
</odoo>
