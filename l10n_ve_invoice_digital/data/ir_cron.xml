<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <record id="ir_cron_purge_tfhka_api_log" model="ir.cron">
            <field name="name">The Factory HKA: Purge API Log History</field>
            <field name="model_id" ref="model_tfhka_api_log"/>
            <field name="state">code</field>
            <field name="code">model.cron_purge_old_logs()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="nextcall" eval="(DateTime.now() + relativedelta(days=1)).strftime('%Y-%m-%d 00:00:00')"/>
            <field name="active" eval="True"/>
            <field name="user_id" ref="base.user_root"/>
        </record>

        <record id="ir_cron_tfhka_digitalization_queue" model="ir.cron">
            <field name="name">The Factory HKA: Process Digitalization Queue</field>
            <field name="model_id" ref="model_account_move"/>
            <field name="state">code</field>
            <field name="code">env['account.move']._tfhka_cron_process_queue_multi(['account.move', 'account.retention', 'stock.picking'])</field>
            <field name="interval_number">1</field>
            <field name="interval_type">minutes</field>
            <field name="nextcall" eval="(DateTime.now() + relativedelta(minutes=1)).strftime('%Y-%m-%d %H:%M:%S')"/>
            <field name="active" eval="True"/>
            <field name="user_id" ref="base.user_root"/>
        </record>
    </data>
</odoo>
