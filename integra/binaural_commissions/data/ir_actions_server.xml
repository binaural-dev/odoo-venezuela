<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="generate_seller_in_invoice" model="ir.actions.server">
        <field name="name">Pay Commissions</field>
        <field name="model_id" ref="account.model_account_move"/>
        <field name="binding_model_id" ref="account.model_account_move"/>
        <field name="state">code</field>
        <field name="code">
action = records.generate_seller_in_invoices()
        </field>
    </record>

    <record id="action_set_commission_from_sale" model="ir.actions.server">
        <field name="name">Set Commission from Sale</field>
        <field name="model_id" ref="sale.model_sale_order"/>
        <field name="binding_model_id" ref="sale.model_sale_order"/>
        <field name="state">code</field>
        <field name="code">
action = records.set_commission_from_sale()
        </field>
    </record>

    <record id="action_calculate_commission" model="ir.actions.server">
        <field name="name">Calculate Commission</field>
        <field name="model_id" ref="account.model_account_move"/>
        <field name="binding_model_id" ref="account.model_account_move"/>
        <field name="state">code</field>
        <field name="code">
action = records.calculate_commission()
        </field>
    </record>
</odoo>
