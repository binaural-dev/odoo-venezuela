/** @odoo-module **/

import BarcodeQuantModel from '@stock_barcode/models/barcode_quant_model';

export default class BinauralBarcodeQuantModel extends BarcodeQuantModel {
  constructor(params) {
    super(...arguments);
  }
}
