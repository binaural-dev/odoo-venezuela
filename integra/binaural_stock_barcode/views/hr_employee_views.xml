<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_employee_form" model="ir.ui.view">
        <field name="name">hr.employee.form</field>
        <field name="model">hr.employee</field>
        <field name="inherit_id" ref="hr.view_employee_form" />
        <field name="arch" type="xml">
            <xpath expr="//field[@name='coach_id']" position="after">
                <field name="role_picking" />
                <field name="supervisor_barcode_password" 
                    attrs="{'invisible': [('role_picking','!=', 'supervisor')]}"/>
            </xpath>
            <xpath expr="//notebook" position="inside">
                <page string="Pick Information" attrs="{'invisible': [('role_picking','!=', 'picker')]}">
                    <field 
                        name="pick_ids" 
                        editable="bottom" 
                        domain="[('operation_state','!=','ready')]">
                        <tree>
                            <field name="operation_state"/>
                            <field name="name"/>
                        </tree>
                    </field>
                </page>
            </xpath>
        </field>
    </record>
</odoo>
