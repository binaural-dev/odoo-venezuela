<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <template id="portal_my_quotations_3" inherit_id="sale.portal_my_quotations">
        <xpath expr="//t[1]/t[2]" position="inside">
            <div class="px-2" t-if="request.env.user.employee_id.is_seller">
                <a t-attf-href="/budget" class="btn btn-primary btn-sm"> Create budget</a>
            </div>
            <div class="px-2" t-if="search">
                <a t-attf-href="/my/quotes" class="btn btn-secondary btn-sm">Clean search</a>
            </div>
        </xpath>
    </template>

    <template id="portal_my_quotations_1" inherit_id="sale.portal_my_quotations">
        <xpath expr="//t[3]/thead" position="replace">
            <t t-if="request.env.user.employee_id.is_seller">
                <thead>
                    <tr class="active">
                        <th>Number</th>
                        <th class="text-end">Customer</th>
                        <th class="text-end">Date</th>
                        <th class="text-end">Total.</th>
                    </tr>
                </thead>
            </t>
            <t t-else="">
                <thead>
                    <tr class="active">
                        <th>Quotation #</th>
                        <th class="text-end">Quotation Date</th>
                        <th class="text-end">Valid Until</th>
                        <th class="text-center"/>
                        <th class="text-end">Total.</th>
                    </tr>
                </thead>
            </t>            
        </xpath>
    </template>

    <template id="portal_my_quotations_2" inherit_id="sale.portal_my_quotations">
        <xpath expr="//t[3]/t/tr" position="replace">
            <t t-if="request.env.user.employee_id.is_seller">
                <tr>
                    <td><a t-attf-href="/budget-{{quotation.id}}"><t t-out="quotation.name"/></a></td>
                    <td class="text-end"><span t-field="quotation.partner_id.name"/></td>
                    <td class="text-end">
                        <span t-field="quotation.create_date" t-options="{'widget': 'date'}"/>&amp;nbsp;
                        <span class='d-none d-md-inline'/>
                    </td>
                    <td class="text-end">
                        <span t-field="quotation.amount_total"/>
                    </td>
                </tr>
            </t>
            <t t-else="">
                <tr>
                    <td><a t-att-href="quotation.get_portal_url()"><t t-out="quotation.name"/></a></td>
                    <td class="text-end"><span t-field="quotation.date_order"/></td>
                    <td class="text-end"><span t-field="quotation.validity_date"/></td>
                    <td class="text-center">
                        <span t-if="quotation.state == 'cancel'" class="badge rounded-pill text-bg-secondary">
                            <i class="fa fa-fw fa-remove"/> Cancelled</span>
                        <span t-if="quotation.is_expired" class="badge rounded-pill text-bg-secondary">
                            <i class="fa fa-fw fa-clock-o"/> Expired</span>
                    </td>
                    <td class="text-end">
                        <span t-field="quotation.amount_total"/>
                    </td>
                </tr>
            </t>
        </xpath>
    </template>


    <template id="portal_my_orders_3" inherit_id="sale.portal_my_orders">
        <xpath expr="//t[1]/t[2]" position="inside">
            <div class="px-2" t-if="search">
                <a t-attf-href="/my/orders" class="btn btn-primary btn-sm">Clean search</a>
            </div>
        </xpath>
    </template>

    <template id="portal_my_orders_1" inherit_id="sale.portal_my_orders">
        <xpath expr="//t[3]/thead" position="replace">
            <t t-if="request.env.user.employee_id.is_seller">
                <thead>
                    <tr class="active">
                        <th>
                            <span class='d-none d-md-inline'>Number</span>
                            <span class='d-block d-md-none'>Num.</span>
                        </th>
                        <th class="text-center">Customer</th>
                        <th class="text-end">Date</th>
                        <th class="text-end">Total.</th>
                    </tr>
                </thead>
            </t>
            <t t-else="">
                <thead>
                    <tr class="active">
                        <th>
                            <span class='d-none d-md-inline'>Sales Order #</span>
                            <span class='d-block d-md-none'>Ref.</span>
                        </th>
                        <th class="text-end">Date</th>
                        <th class="text-center"/>
                        <th class="text-end">Total.</th>
                    </tr>
                </thead>
            </t>  
        </xpath>
    </template>

    <template id="portal_my_orders_2" inherit_id="sale.portal_my_orders">
        <xpath expr="//t[3]/t/tr" position="replace">
            <t t-if="request.env.user.employee_id.is_seller">
                <tr>
                    <td><a t-attf-href="/budget-{{order.id}}"><t t-out="order.name"/></a></td>
                    <td class="text-center">
                        <span t-field="order.partner_id.name"/>
                    </td>
                    <td class="text-end">
                        <span t-field="order.date_order" t-options="{'widget': 'date'}"/>&amp;nbsp;
                        <span class='d-none d-md-inline'/>
                    </td>
                    <td class="text-end"><span t-field="order.amount_total"/></td>
                </tr>
            </t>
            <t t-else="">
                <tr>
                    <td><a t-att-href="order.get_portal_url()"><t t-out="order.name"/></a></td>
                    <td class="text-end">
                        <span t-field="order.date_order" t-options="{'widget': 'date'}"/>&amp;nbsp;
                        <span class='d-none d-md-inline' t-field="order.date_order" t-options="{'time_only': True}"/>
                    </td>
                    <td class="text-center">
                        <span t-if="order.state == 'done'"  class="badge rounded-pill text-bg-success">
                            <i class="fa fa-fw fa-check" role="img" aria-label="Done" title="Done"/>Done
                        </span>
                    </td>
                    <td class="text-end"><span t-field="order.amount_total"/></td>
                </tr>
            </t>  
        </xpath>
    </template>

</odoo>
